#include <stdio.h>

void main() {

	int my_age;         // this variable is not initialized so we don’t know it’s value
	int your_age = 20;  // this variable is initialized to 20

	float net_price, gross_price;      // two variables declared in one statement
	short age1 = 1, age2 = 2, age3 = 3;  // declare and initialize in one statement
	char first_initial;

	printf("my_age is: %d\n", my_age); // what do you notice about it's value when printed?
	printf("your_age is: %d\n", your_age);


	/**
	 * TODO
	 * 1.) assign the values 9.99 and 12.99 to net_price and gross_price below
	 * 2.) print the values of net_price and gross_price
	 * 3.) print age1, age2 and age3 below
	 * 4.) assign the appropriate value to first_initial and print it below
	 */
}
